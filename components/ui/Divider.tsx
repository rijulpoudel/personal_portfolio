export default function Divider() {
  return (
    <hr
      style={{
        border: "none",
        borderTop: "1px solid var(--border)",
        marginBlock: "var(--spacing-lg, 4rem)",
      }}
    />
  );
}
